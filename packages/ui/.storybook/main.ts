import type { StorybookConfig } from '@storybook/react-vite';
import { createStorybookMainConfig } from '@open20/config/storybook';

const config: StorybookConfig = createStorybookMainConfig();

export default config;
